#pragma once

// Function Declarations
int fSum(int a, int b = 0); // b set to 0 by default if no b is passed to func
vector<int> fUnitVec(int size);
void fPrintVec(vector<int> v);
string fConStr(const string& s1, const string& s2);
string fType(int num);
string fType(const string& str);